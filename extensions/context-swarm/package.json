{
  "name": "context-swarm",
  "version": "0.1.0",
  "description": "Firefox extension for agent-driven context harvesting — parallel tab extraction to LLM-ready markdown",
  "private": true,
  "scripts": {
    "dev": "./scripts/dev.sh",
    "build": "./scripts/build.sh"
  },
  "devDependencies": {
    "web-ext": "^8.4.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "turndown": "^7.2.0",
    "turndown-plugin-gfm": "^1.0.2"
  }
}
